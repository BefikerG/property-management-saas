
# ⚠️ Generated Code — Do Not Hand-Edit

This directory is generated by `npm run generate:api` from the backend's
live OpenAPI spec at `http://localhost:8080/v3/api-docs`.

**Regenerate this client any time a backend controller's request/response
DTO, endpoint path, or method signature changes.** Committed to git for
now (pre-CI/CD, Phase 2D) so the frontend build doesn't require Java or
a running backend. This will be automated in Phase 2E's CI/CD pipeline
(regenerate-on-push), at which point this manual step and this file
can be removed.

Checklist trigger: if you touch any file under
`src/main/java/com/propmanager/modules/**/controller/` or
`src/main/java/com/propmanager/modules/**/dto/` on the backend,
run `npm run generate:api` before your next frontend commit.

